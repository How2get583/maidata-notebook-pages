# Maidata Notebook / 设计说明

## 方向

个人博客式的工具笔记，而不是游戏宣传页。视觉采用石墨黑、冷灰、薄荷绿
和少量琥珀色；装饰只用细线、等宽字体和清晰的状态标识，重点放在文章内容、
筛选条件和载入动作。

## 页面结构

- 左侧是可随页面滚动的介绍、筛选器和配置文章列表。
- 右侧是 `position: sticky` 的 Viewer 卡片，固定在当前视口内。
- Viewer 的当前条目、状态和控制在同一张卡内，避免用户误以为浏览文章会
  自动切换谱面。
- 不提供移动端重排；首版明确以桌面宽度为目标。

## 交互原则

- 标签筛选是 AND 关系；所有标签按钮始终可见。
- BPM 的两个输入框对应闭区间上下界，空值表示该侧不限。
- 默认最新添加在前，排序选项只改变文章顺序，不改变当前 Viewer。
- “载入”按钮是唯一切换 Viewer 内容的文章动作。
- 网页通过 `postMessage` 向独立 Viewer 发送文本和控制命令，不重载 iframe。
- 正解音、暂停、重播和速度控制仍由 Unity Viewer 处理。

## 内容状态

文章里的 maidata 采用代码块呈现，不做语法高亮，避免短配置被装饰掩盖。
每条文章底部并列展示标签、来源图标、复制和载入动作。没有匹配项时给出
明确的筛选回退提示，而不是空白区域。

## 实施限制

- 根目录站点无第三方依赖、无构建步骤。
- Viewer 保留 `MajdataView_web` 的 GPL-3.0 源码与原生 Note 生成路径，
  只在输入和媒体生命周期处做最小改动。
- 博客仓库与 Viewer 仓库分离；博客只需要 `viewerUrl` 配置。

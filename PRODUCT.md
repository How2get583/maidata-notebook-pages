# Maidata Notebook / 产品约束

## 定位

这是一个给舞萌自制谱师使用的个人配置索引。它记录的是几行 maidata、一个
BPM 和一个配置说明，不承担完整曲库、音乐播放器或社交投稿系统的职责。

## 核心流程

1. 浏览条目或选择一个或多个标签。
2. 可选填写 BPM 下限和上限；范围是闭区间，`120—120` 表示精确查找。
3. 按默认的“最新添加”或其他排序方式浏览。
4. 只有点击某条文章的“载入”按钮时，右侧 Viewer 才切换到该条片段。
5. Viewer 默认开启正解音，不读取 MP3、BG 或 Track；播放时保留原生
   暂停/继续、重播、速度调整和设置入口。

## 数据边界

- 站点是静态 GitHub Pages；条目由 `notes.js` 管理并随仓库提交发布。
- 当前只有仓库拥有者管理内容，没有登录、数据库或公开投稿流程。
- 条目包含标题、简介、完整标签、BPM、短 maidata、添加日期和一个来源链接。
- Viewer 直接消费文本，并自动补独立 `E` 作为短片段结束标记。
- 暂不处理变速片段、音轨、背景图、手机端布局和全文搜索。

## 非目标

- 不重新实现 Simai 渲染器。
- 不在博客端绘制“近似谱面”代替原生 Viewer。
- 不引入 npm、前端框架或必须运行的后端服务。
